import { type ReactElement } from 'react'
import { Button } from 'react-bootstrap'

export const BecomeOwnerButton = (): ReactElement => {
  return (
    <Button variant="info">
      Become Owner
    </Button>
  )
}